using System;
class JomoPipi
{
   public static int[] MutationLocation(char[][] body)
   {
    if (body.readLine() == null)
    {
      return new int[0];
    } 
     
    String s;
     
    while ((s = body.readLine()) != null) 
    {
      for (int i = 0; i < body.Length; i++)
      {
        
      }
    }
}


// Passo 1: Ler cada linha da matriz
// Passo 2: Compara uma linha com a próxima
// Passo 3: Pegue a linha que está diferente
// Passo 4: Veja a forma de imprimir a posição